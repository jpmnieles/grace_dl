_target_: src.data.grace_datamodule.GraceDataModule
csv_fn: "dataset_left_chest_20240627_162502_402545.csv"
data_dir: ${paths.data_dir}
batch_size: 32 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 15
pin_memory: False
