_target_: src.data.grace_datamodule.GraceDataModule
data_dir: "20240715_032420_095729"
eye: "left"
val_size: 0.2
batch_size: 64 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 15
pin_memory: False
